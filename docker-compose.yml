services:
  app:
    build: .
    ports:
      - "8501:8501"
    environment:
      # Ollama host makinede çalışıyorsa bu ayar gerekli
      - OLLAMA_HOST=http://host.docker.internal:11434
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
